{
    "name": "RegexReplace",
    "description": "Option to allow matching and replacing against incoming messages",
    "disable": false,
    "trigger": "message",
    "scope": "before",
    "config": [
            {
                "name": "regexReplaceMatchRegex",
                "label": "Regex to match with",
                "description": "If the message content matches this regex pattern, it's matching part will be replaced with the below text.",
                "type": "text"
            },
            {
                        "name": "regexReplaceString",
                        "label": "Replacement test for matched Regex",
                        "description": "This string will be what replaces the matched regex sections.",
                        "type": "text"
            }
    ],
    "aliasConfig": [
    ]
}
