{
    "name": "Shell",
    "description": "Exec shell script",
    "disable": false,
    "trigger": "message",
    "scope": "after",
    "config": [
    ],
    "aliasConfig": [ 
        {
            "name": "enable",
            "label": "Enable",
            "description": "Enable shell script that match this alias. File must be named {ID}.sh (Linux) or {ID}.ps1 (Windows), replace \"ID\" by alias id. ",
            "type": "checkbox"
        },
        {
            "name": "overrideAlias",
            "label": "Override Alias",
            "description": "Override filename, use another alias id. (only number) ",
            "type": "number"
        }
    ]
}