{
    "name": "MicrosoftTeams",
    "description": "Push messages to Microsoft Teams Channel",
    "disable": false,
    "trigger": "message",
    "scope": "after",
    "config": [
    ],
    "aliasConfig": [
        {
            "name": "enable",
            "label": "Enable",
            "description": "Send matching messages to your Microsoft Teams Channel",
            "type": "checkbox"
        },
        {
            "name": "webhookuri",
            "label": "Webhook URI",
            "description": "Webhook URI to post too",
            "type": "text",
            "required": true
        }
    ]
}